<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zaze">

    <permission android:name="com.zaze.receiver.RECEIVER" />
    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".component.animation.ui.AnimationActivity" />

        <activity android:name=".component.rxandroid.RxAndroidActivity" />

        <activity
            android:name=".component.toolbar.ToolBarDemoActivity"
            android:theme="@style/AppTheme.NoActionBar" />


        <!--测试接收推送-->
        <service android:name=".component.message.service.MyService" />
        <receiver
            android:name=".component.message.receiver.TestBroadcastReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.xh.message.testappid" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
